// Operador Condicional ternário
let age = 18
console.log(age >=18 // Condição
    ? "Você pode dirigir." // Se verdadeiro (True)
    :"Você não pode dirigir.") // Se falso (False)

    console.log(
        age <18
        ? "Você não pode dirigir."
        : "Você pode dirigir." 
    )